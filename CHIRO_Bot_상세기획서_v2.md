# CHIRO Bot — 대화형 AI 스케줄러 비서 상세 기획서 v2.0

> 2026년 3월 31일

---

## 1. 프로젝트 개요

### 1.1 문제 정의

까먹음과 합리화로 중요한 일을 미루며, 결과적으로 졸업까지 밀림. 기존 툴들(투두리스트, 캘린더, 로컬 봇)은 모두 사용자가 먼저 접근해야 작동하는 구조라서 전부 실패함.

### 1.2 핵심 솔루션

봇이 먼저 찾아와 묻고, 완료까지 추적하며, 미완료 시 재일정을 잡는 대화형 텔레그램 비서 봇. 사용자가 가는 것이 아니라 봇이 먼저 온다.

### 1.3 아이디어 카드

| 항목 | 내용 |
|------|------|
| **문제** | 까먹음과 합리화로 중요한 일을 미루어 졸업이 밀림 |
| **해결책** | 봇이 먼저 찾아와 묻고, 완료까지 추적하는 대화형 비서 |
| **타겟** | 나 자신 (CHIRO) |
| **비용 구조** | Oracle Cloud 무료 + 무료/저가 AI API |
| **핵심 차별화** | 내가 가는 게 아니라 봇이 먼저 온다 |
| **가장 큰 리스크** | 봇이 예측 가능해져서 무시하게 됨 / 봇이 있어도 행동이 안 바뀜 |

### 1.4 기술 스택

*(원본 문서에 상세 내용 참조)*

---

## 2. 시스템 아키텍처

### 2.1 전체 구성도

시스템은 4개의 구성요소로 이루어진다:

1. **사용자** — 스마트폰 Telegram 앱
2. **Telegram 서버** — 메시지 중계
3. **Oracle Cloud 서버** — 봇 엔진 + DB, 24시간 ON
4. **AI API** — 외부 호출, 인텐트 파싱 + 메시지 생성

### 2.2 통신 흐름

#### 2.2.1 리액티브 (유저 → 봇)

유저가 Telegram에 메시지 전송 → Telegram 서버가 Webhook으로 Oracle Cloud 서버에 전달 → 리액티브 핸들러가 DB 조회 + AI API 호출로 응답 생성 → Telegram Bot API로 응답 발송

#### 2.2.2 프로액티브 (봇 → 유저)

APScheduler가 10-20분 주기로 함수 트리거 → DB에서 오늘의 태스크/플랜/DND/미응답 전체 로드 → 7단계 우선순위 판단 → AI API로 메시지 생성 → Telegram Bot API로 발송 → DB에 발송 기록

### 2.3 보안/안정성

- **헬스체크**: 봇이 매일 정해진 시간에 무조건 메시지를 하나 보냄. 이것이 오지 않으면 서버가 죽은 것.
- **DB 백업**: SQLite 파일을 일간 백업 (서버 내 별도 디렉토리 또는 외부 저장소)

---

## 3. 봇 구동 엔진 (2개)

### 3.1 프로액티브 루프 (봇이 먼저 동작)

APScheduler가 10-20분 주기로 트리거하며, 유저가 아무것도 하지 않아도 혼자 돌아간다.

#### 3.1.1 실행 순서

1. **웨이크업**
2. DB 전체 조회 (tasks, plan, dnd_slots, messages, patterns)
3. **7단계 판단 캐스케이드**
4. AI API 호출로 메시지 생성
5. Telegram 발송
6. DB에 발송 기록
7. Sleep

#### 3.1.2 7단계 판단 캐스케이드 (우선순위 순서)

각 단계는 Y/N으로 분기된다. Y이면 해당 액션을 실행하고, N이면 다음 단계로 내려간다.

### 3.2 리액티브 핸들러 (유저 메시지 수신 시)

Telegram Webhook으로 유저 메시지 수신 시 작동한다.

#### 3.2.1 실행 순서

1. 메시지 수신
2. DB에서 유저 컨텍스트 로드 (current_task, plan, 마지막 봇 메시지, 카운터, user_state)
3. AI API 인텐트 파싱
4. **인텐트 라우팅**
5. DB 업데이트
6. AI API 응답 생성
7. Telegram 발송
8. 상호작용 로그 기록

#### 3.2.2 인텐트 라우팅 (3갈래)

*(원본 문서에 상세 분기 참조)*

---

## 4. 하루 전체 사이클

### 4.1 메인 플로우

*(원본 문서에 상세 플로우 참조)*

### 4.2 인터럽트 채널 (3개)

모니터링 중 언제든 끼어들 수 있는 경로:

1. **긴급 일정 삽입**: 기존 플랜 시간을 자동으로 뒤로 밀고 재조정
2. **중간 태스크 추가**: 수집(시간/마감) → 우선순위 재계산 → 플랜 재조정
3. **아이디어 캡처**: 태스크 방해 없이 ideas 테이블에 저장. 추후 태스크로 전환 가능

### 4.3 방해금지 (DND) 처리

두 가지 경로로 DND가 등록된다:

- **아침 수집 시**: Q5에서 "못 보는 시간대 있어?" → dnd_today에 등록
- **실시간 추가**: 모니터링 중 "수업 중이야" → 즉시 dnd_today에 추가 + 플랜 재조정

DND 시간에는 프로액티브 루프가 7단계 판단 3번에서 차단되어 조용히 대기한다.

---

## 5. 태스크 수집 흐름 (5단계 질문)

봇이 먼저 묻고, 유저는 대답만 한다. 모호한 답변은 재질문으로 잡는다.

수집 완료 후: 봇이 마감 + 소요시간 + 시작시간 + DND를 기반으로 **플랜 자동 생성** → 유저에게 공유 → 확인/수정 후 확정.

### 5.1 플랜 생성 로직

- 마감이 빠른 태스크부터 배치 (긴급도 우선)
- DND 시간대는 빈 슬롯으로 처리 (태스크 배치 불가)
- routines (평소 수업 시간표)와 충돌 체크
- 유저가 명시한 시작 시간 반영

---

## 6. 태스크 상태 분기 (5갈래)

유저가 진행 상황에 대해 응답하면, 다섯 가지 상태 중 하나로 분류된다.

### 6.1 반복 실패 감지

- **미룸 3회 이상**: 봇 메시지 톤이 압박으로 변경된다. 구체적으로 남은 시간과 마감을 명시하며 즉시 실행을 요구한다.
- **못함 3회 이상**: 태스크 자체를 더 작은 단위로 분해할 것을 제안한다. 큰 태스크가 실행 장벽을 만드는 경우에 대한 대응.

### 6.2 완료 후 다음 태스크 연결

태스크 완료 시 봇이 daily_plan에서 다음 태스크를 찾아 자동으로 제안한다. 남은 태스크가 없으면 저녁 리뷰로 넘어간다.

---

## 7. 데이터베이스 스키마 (3계층)

### 7.1 고정 데이터 (거의 안 바뀌는 것)

평소 루틴과 선호 설정. 봇이 플랜을 짤 때 기본 참조하는 데이터.

### 7.2 일간 데이터 (매일 생성/소멸)

오늘의 플랜과 실시간 상태. 하루가 끝나면 누적 데이터로 아카이브된다.

### 7.3 누적 데이터 (계속 쌓임)

완료된 기록과 행동 패턴. 시간이 지나면 "까먹을 만한 시간"을 예측하는 데 활용된다.

### 7.4 데이터 흐름

- **플랜 생성 시**: 고정 데이터(routines, dnd_defaults) + 일간 데이터(daily_tasks) → 충돌 체크 → daily_plan 생성
- **하루 끝 (저녁 리뷰 후)**: 일간 데이터 → 누적 데이터로 아카이브 (task_history, daily_reports)
- **패턴 업데이트**: 누적 데이터(task_history, action_log) → patterns 테이블 갱신
- **패턴 피드백**: patterns → 다음날 플래너 판단에 반영 (미루는 시간대, 카테고리별 완료율 등)

---

## 8. 에스컬레이션 및 압박 설계

### 8.1 미응답 에스컬레이션

봇이 메시지를 보냈는데 유저가 응답하지 않으면 간격이 점점 줄어든다.

### 8.2 마감 기반 압박

태스크의 마감까지 남은 시간이 소요시간 이하로 떨어지면 긴급 리마인더가 발동된다. 남은 시간과 마감을 구체적 수치로 명시하며 즉시 실행을 요구한다.

### 8.3 반복 실패 대응

- **미룸 3회 이상** 반복 시 봇의 메시지 톤이 압박으로 변경된다.
- **못함 3회 이상** 반복 시 태스크를 더 작은 단위로 분해하는 것을 제안한다.

---

## 9. 시뮬레이션 검증 결과

3회의 시뮬레이션을 통해 설계의 완전성을 검증했다.

### 9.1 시뮬레이션 1: 순조로운 날

- **검증 항목**: 태스크 수집 5단계, 플랜 생성/확인, 리마인더, 모호한 답변 재질문, DND 차단, 완료 후 다음 태스크 연결
- **결과**: 전부 정상 작동

### 9.2 시뮬레이션 2: 미루고 무시하는 날

- **검증 항목**: 에스컬레이션 간격 축소, 모호한 답변 재질문, 미룸 상태 3회 카운트 + 압박 강화, 부분완료 분리
- **결과**: 전부 정상 작동

### 9.3 시뮬레이션 3: 예외 폭탄

- **검증 항목**: 긴급 일정 삽입 + 플랜 재조정, 중간 태스크 추가 + 우선순위 재계산, 실시간 DND 추가 + 플랜 재조정
- **결과**: 전부 정상 작동
- **추가 발견**: 실시간 DND 추가 경로 (긴급 일정 삽입과 동일 경로로 처리)

---

## 10. 데스 테스트 결과 및 리스크

*(원본 문서에 상세 내용 참조)*

---

## 11. 빌드 단계 제안

### Phase 1 — 최소 실행 단위 (2주)

비서가 먼저 말을 거는 기능 하나만 구현하여 실제로 쓴다.

- [ ] Telegram 봇 생성 + Webhook 연결
- [ ] Oracle Cloud 서버 세팅 + APScheduler 기본 구동
- [ ] SQLite DB 초기화 (daily_tasks, daily_plan, messages)
- [ ] 아침 시작 메시지 + 태스크 수집 흐름 (5단계)
- [ ] 플랜 생성 + 유저 확인
- [ ] 시작 리마인더 발송 (프로액티브 루프 기본)

### Phase 2 — 모니터링 엔진 (+2주)

- [ ] 7단계 판단 캐스케이드 전체 구현
- [ ] 태스크 상태 분기 5갈래 처리
- [ ] 에스컬레이션 + 반복 실패 감지
- [ ] DND 처리 (기본 + 실시간)
- [ ] 저녁 보고서 + 일간 아카이브
- [ ] 헬스체크 장치

### Phase 3 — 확장 (+지속)

- [ ] 긴급 일정 삽입 / 중간 태스크 추가
- [ ] 고정 데이터(routines) 등록 기능
- [ ] 패턴 학습 및 피드백
- [ ] 아이디어 볼트
- [ ] 페르소나 고도화

---

## 12. 미결 사항

*(원본 문서에 상세 내용 참조)*

---

> **이 문서는 빌더에게 전달할 완전한 설계 명세서이다.**
> 코드는 포함되지 않으며, 이 기획서를 바탕으로 빌드를 진행한다.
